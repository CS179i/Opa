<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.opa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_opa_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_opa_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"/>
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login_register.LandingPageActivity"/>
        <activity android:name=".login_register.GuestLoginActivity"/>
        <activity android:name=".login_register.RegisteredUserLoginActivity"/>
        <activity android:name=".login_register.OrganizerLoginActivity"/>
    </application>

</manifest>